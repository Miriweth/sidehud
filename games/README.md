# Games

One folder per game integration, each with its own README. `stardew/` is the
reference. What a new integration has to send and how its panel works is in
[docs/plugin-spec.md](../docs/plugin-spec.md).
